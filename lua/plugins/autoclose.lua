local M = {}

M.install = {
  -- 'm4xshen/autoclose.nvim',
}

M.after = function()
  -- local status_ok, plugin = pcall(require, 'autoclose')
  -- if not status_ok then
  --   return
  -- end
  --
  -- plugin.setup {
  --   -- keys = {
  --   --   ['>'] = { escape = false, close = false, pair = '<>', disabled_filetypes = {} },
  --   -- },
  -- }
end

return M
